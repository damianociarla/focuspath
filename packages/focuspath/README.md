# FocusPath

See where keyboard navigation breaks. FocusPath presses `Tab` through a real page, records the focus sequence, flags deterministic accessibility problems, and generates a visual HTML report.

```bash
npx focuspath https://example.com
```

See the [root README](../../README.md) for documentation and examples.
